#include<bits/stdc++.h>
using namespace std;
class heap{
  int arr[100];
  int size = 0;

  void insert(int val){
     size++;
     int index = size;
     arr[index] = val;

     while(index > index - 1){
        int parent = index /2;
        if(arr[parent] > 4  array)
     }
  }
};


int main(){

  return 0;
}